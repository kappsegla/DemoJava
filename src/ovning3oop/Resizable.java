package ovning3oop;

public interface Resizable {
    /**
     *
     * @param size Change size of both sides where size is new size in percent
     *             value 100 will not change the size of shape
     */
    void resize(int size);
}
